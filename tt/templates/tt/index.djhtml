<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <title>Document</title>
    </head>
    <body>
      <div id="sidebar">
        {% for module in module_list %}
        <div class="moduleTitle">
          <h6>{{ module.title }}</h6>
          {% for activity in module_list.aktivnost_set %}
          <div class="activityTitle">
            <h7>{{activity.title}}</h7>
          </div>
            {% endfor %}
          </div>
        </div>
        {% endfor %}

      </div>
    </body>
</html>
